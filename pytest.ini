[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
python_files = test_*.py
addopts = -ra
markers =
    unit: unit tests for isolated code logic
    integration: narrow integration tests on module boundaries
filterwarnings =
    ignore::DeprecationWarning
